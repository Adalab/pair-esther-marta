import React, { useState } from "react";


  const { name } = users;
  const { time } = users;

  return (
    <>
      <p>
        The third classified is {name} with a time of {time}
      </p>
  {/*     <p>
        The second classified is {second.name} with a time of {second.time}
      </p>
      <p>
        And the last but not the least is our WINNER, the first classified:{" "}
        {first.name} with a time of {first.time}
      </p> */}
    </>
  );
}
export default App;
